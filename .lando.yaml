name: bq
service:
  appserver:
    type: python:3.7
    build:
      - pip install -r requirements.txt
  database:
    type: postgres:10

tooling:
  python:
    service: appserver
  pip:
    service: appserver
  psql:
    service: database
    cmd: psql database -h database -U postgres
